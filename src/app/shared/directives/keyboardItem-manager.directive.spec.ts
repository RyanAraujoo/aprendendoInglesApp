/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { KeyboardItemManagerDirective } from './keyboardItem-manager.directive';
import { ElementRef } from '@angular/core';

describe('Directive: KeyboardItemManager', () => {

  beforeEach(() => {
    TestBed.configureTestingModule({
      imports: [ElementRef]

    });

  it('should create an instance', () => {
    //const element = new ElementRef<HTMLElement>()
    //const directive = new KeyboardItemManagerDirective(element);
    //expect(directive).toBeTruthy();
  });
})
}
